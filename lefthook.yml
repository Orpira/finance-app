pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: npx eslint {staged_files}
    typecheck:
      glob: "*.{ts,tsx}"
      run: npm run typecheck

pre-push:
  commands:
    test:
      run: npm test
