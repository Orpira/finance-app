# Build

Instalar dependencias desde lockfile, ejecutar TypeScript y Vite mediante `npm run build`. No modificar lockfile para resolver fallos ambientales sin una decisión explícita. Android debe sincronizarse únicamente después de un build web correcto.
