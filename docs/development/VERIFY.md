# Verify

Gate objetivo: `npm run verify`. Hasta su implementación, ejecutar `npm test`, `npm run build`, `npm run lint` y `npm run test:indexeddb` cuando el cambio afecte persistencia. Ningún gate puede sustituirse por exclusiones, `skip`, `only`, `ts-ignore` o relajación global.
