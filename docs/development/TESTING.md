# Testing

Orden mínimo: pruebas unitarias del alcance, `npm test`, `npm run build`, lint específico y `git diff --check` cuando exista Git. Los fallos globales históricos se documentan, nunca se silencian. Los contratos puros requieren pruebas de invariantes, inmutabilidad, determinismo y JSON safety.
